name = "jlrdev-website"
pages_build_output_dir = "."

[vars]
# Non-secret env vars go here.
# Secrets (OPENAI_API_KEY, RESEND_API_KEY) are set in:
#   Cloudflare dashboard → Pages → jlrdev-website → Settings → Environment variables
